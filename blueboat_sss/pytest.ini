[pytest]
# Run from BlueBoat-SSS/blueboat_sss/ — the parent of the blueboat_gcs
# package, which is the documented working directory for every GCS
# command (CLAUDE.md, Commands).
testpaths = tests
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
